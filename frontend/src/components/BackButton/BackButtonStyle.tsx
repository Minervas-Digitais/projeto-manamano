/* eslint-disable import/prefer-default-export */
import styled from 'styled-components/native';
import backButton from '../../assets/back-button-icon.svg';

export const BackButtonIcon = styled(backButton)``;
